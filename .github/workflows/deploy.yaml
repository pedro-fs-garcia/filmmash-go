name: Deploy

on:
  workflow_run:
    workflows: ["CI"]
    types: [completed]
    branches: ["main"]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: pedro-fs-garcia/filmmash-go

jobs:
  build-and-push:
    if: github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    outputs:
      # Same value in every matrix leg (derived only from head_sha), so picking
      # whichever leg finishes last to populate the job output is safe.
      sha_short: ${{ steps.vars.outputs.sha_short }}

    strategy:
      matrix:
        component:
          - name: app
            dockerfile: Dockerfile.app
          - name: migrate
            dockerfile: Dockerfile.migrate
          - name: seed
            dockerfile: Dockerfile.seed

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Set image tag
        id: vars
        run: echo "sha_short=${SHA::7}" >> "$GITHUB_OUTPUT"
        env:
          SHA: ${{ github.event.workflow_run.head_sha }}

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ${{ matrix.component.dockerfile }}
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-${{ matrix.component.name }}:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-${{ matrix.component.name }}:sha-${{ steps.vars.outputs.sha_short }}

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Copy compose files to Droplet
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.DROPLET_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          target: /opt/filmmash
          source: docker-compose.yaml,docker-compose.prod.yaml,monitoring/alloy,nginx
          debug: true
          timeout: 15s
          command_timeout: 2m

      - name: Deploy to Droplet
        uses: appleboy/ssh-action@v1.0.3
        env:
          IMAGE_TAG: sha-${{ needs.build-and-push.outputs.sha_short }}
        with:
          host: ${{ secrets.DROPLET_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          envs: IMAGE_TAG
          script: |
            set -e
            cd /opt/filmmash
            COMPOSE="docker compose -f docker-compose.yaml -f docker-compose.prod.yaml"
            $COMPOSE pull app migrate seed
            $COMPOSE run --rm --no-deps migrate
            if [ ! -f .seeded ]; then
              $COMPOSE run --rm --no-deps seed
              touch .seeded
            fi
            $COMPOSE up -d --force-recreate --wait --wait-timeout 60 --no-deps app
            $COMPOSE up -d --force-recreate --wait --wait-timeout 60 nginx alloy
            docker image prune -af