version: "2"
linters:
  enable:
    - lll
  settings:
    lll:
      line-length: 120
      tab-width: 4
    errcheck:
      exclude-functions:
        - (io.Closer).Close
issues:
  max-same-issues: 0
  max-issues-per-linter: 0
